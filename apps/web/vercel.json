{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd ../.. && corepack enable && corepack prepare pnpm@10.0.0 --activate && pnpm install --frozen-lockfile",
  "buildCommand": "prisma generate && next build",
  "framework": "nextjs"
}
